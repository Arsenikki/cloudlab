apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: pangolin-stack
  namespace: argocd
spec:
  project: default
  source:
    repoURL: "https://github.com/bjw-s-labs/helm-charts"
    chart: app-template
    targetRevision: 4.2.0
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: pangolin
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
